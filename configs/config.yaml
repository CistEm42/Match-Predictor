project_name: Football Match Intelligence

random_seed: 42

data:
  raw_path: data/raw
  processed_path: data/processed

model:
  target_outcome: FTR
  target_goals: FTHG

training:
  test_size: 0.2

mlflow:
  experiment_name: football_prediction